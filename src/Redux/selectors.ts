export const selectNotifications = (state) => state.notifications;

export const selectAnnouncements = (state) => state.announcements;

export const selectKycVerification = (state) => state.kycVerification;

export const selectProfile = (state) => state.profile;

export const selectSetting = (state) => state.setting;

export const selectAnalytics = (state) => state.analytics;
